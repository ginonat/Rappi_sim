# ToDo

- Logistics fee is currently a flat rate * distance. Making it respond to supply and
  demand (fewer idle runners / more open requests -> higher fee) is deferred until the
  current flat-rate model has been lived with a bit.
